# ─────────────────────────────────────────────────────────────────────────────
# packages.conf — 需要编译的 PassWall 依赖包列表
# packages.conf — List of PassWall dependency packages to build
#
# 每行一个包名，行首 # 为注释，空行忽略
# One package per line. Lines starting with # are comments.
# 此列表仅用于生成 .config，包的实际工具链分组见 compile-packages.sh
# This list is for .config only; toolchain grouping is in compile-packages.sh
# ─────────────────────────────────────────────────────────────────────────────

chinadns-ng
dns2socks
geoview
hysteria
ipt2socks
microsocks
naiveproxy
shadow-tls
shadowsocks-rust-sslocal
shadowsocks-rust-ssserver
shadowsocksr-libev-ssr-local
shadowsocksr-libev-ssr-redir
shadowsocksr-libev-ssr-server
simple-obfs-client
sing-box
tcping
trojan-plus
tuic-client
v2ray-geoip
v2ray-geosite
v2ray-plugin
xray-core
xray-plugin
