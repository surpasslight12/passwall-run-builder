#!/usr/bin/env bash

# OpenWrt SDK 配置 (使用 25.12 版本以获取更新的工具链)
# OpenWrt SDK configuration (using 25.12 for newer toolchains)
# 请到 https://downloads.openwrt.org/releases/25.12/targets/<target>/<subtarget>/ 页面
# 找到与你设备架构对应的 openwrt-sdk-25.12-*.tar.zst 下载链接，完整替换下面这一行
# For 25.12 SDK, visit https://downloads.openwrt.org/releases/25.12/targets/<target>/<subtarget>/
# and replace the URL below with the SDK matching your device architecture
OPENWRT_SDK_URL=https://downloads.openwrt.org/releases/25.12.0-rc4/targets/x86/64/openwrt-sdk-25.12.0-rc4-x86-64_gcc-14.3.0_musl.Linux-x86_64.tar.zst

# （可选）指定 openwrt-passwall luci 源码的分支 / tag / commit，用于固定 PassWall 版本
# 不设置时默认使用上游仓库当前最新版本
# 例如：
# PASSWALL_LUCI_REF=v26.1.21
#PASSWALL_LUCI_REF=
