#!/usr/bin/env bash

# OpenWrt SDK 配置示例（已切换为 24.10，占位路径）
# 请到 https://downloads.openwrt.org/releases/24.10/targets/<target>/<subtarget>/ 页面
# 找到与你设备架构对应的 openwrt-sdk-24.10-*.tar.xz 下载链接，完整替换下面这一行
OPENWRT_SDK_URL=https://downloads.openwrt.org/releases/24.10.5/targets/x86/generic/openwrt-sdk-24.10.5-x86-generic_gcc-13.3.0_musl.Linux-x86_64.tar.zst

# （可选）指定 openwrt-passwall luci 源码的分支 / tag / commit，用于固定 PassWall 版本
# 不设置时默认使用上游仓库当前最新版本
# 例如：
# PASSWALL_LUCI_REF=v26.1.21
#PASSWALL_LUCI_REF=
